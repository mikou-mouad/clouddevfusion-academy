; Limites pour les articles blog avec contenu riche (images base64, HTML long).
; Nécessaire quand on crée un article depuis l'admin avec beaucoup de contenu.
; Si symfony serve n'utilise pas ce fichier, augmenter post_max_size dans le php.ini système.
post_max_size = 32M
upload_max_filesize = 32M
